{
  "name": "ragahupati-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ../raghupati-frontend && npm ci && npm run build && rm -rf ../ragahupati-frontend/.next && cp -a .next ../ragahupati-frontend/",
    "start": "cd ../raghupati-frontend && npm run start"
  },
  "engines": {
    "node": "20.x"
  }
}
